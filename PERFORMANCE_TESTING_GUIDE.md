# 📏 МЕТРИКИ И СПОСОБ ИЗМЕРЕНИЯ ПРОИЗВОДИТЕЛЬНОСТИ

**Цель:** Обеспечить объективное измерение производительности до и после оптимизации

---

## 🔧 ИНСТРУМЕНТЫ ПРОФИЛИРОВАНИЯ

### Chrome DevTools Performance

```
1. Открыть DevTools (F12)
2. Performance таб
3. Нажать на красный circle (запись)
4. Выполнить экшен (pan/zoom на 100+ маркерах)
5. Остановить запись
6. Анализировать:
   - Framerate (должно быть 60, не падать ниже 50)
   - Main thread usage (должно быть <50%)
   - Paint time (должно быть <5ms)
```

### Chrome DevTools Rendering

```
1. DevTools → Rendering таб
2. Включить "Paint flashing"
3. Включить "Rendering stats"
4. Pan/zoom и смотреть:
   - Какие части перерисовываются (paint)
   - Frequency в FPS
```

### Performance Monitor (Chrome)

```
1. Ctrl+Shift+J (Console)
2. Нажать ⋮ → More tools → Performance Monitor
3. Смотреть в real-time:
   - CPU usage
   - Memory
   - DOM nodes count
   - Listeners count
```

---

## 📊 МЕТРИКИ ПРОИЗВОДИТЕЛЬНОСТИ

### Метрика 1: FPS при пании

**Как измерить:**

```javascript
// В консоли браузера:

// Вариант 1: Manual
let frameCount = 0;
let lastTime = performance.now();

function countFrames() {
  frameCount++;
  const now = performance.now();
  if (now - lastTime >= 1000) {
    console.log(`FPS: ${frameCount}`);
    frameCount = 0;
    lastTime = now;
  }
  requestAnimationFrame(countFrames);
}
countFrames();

// Теперь выполните pan/zoom и смотрите FPS в консоли

// Вариант 2: Использовать Chrome DevTools Performance Monitor
// Performance Monitor показывает FPS в real-time
```

**Целевой показатель:**

| Маркеров | Целевой FPS | Минимум | Статус |
|----------|-------------|---------|--------|
| 50-80    | 60          | 50      | ✅ OK |
| 80-100   | 60          | 50      | ⚠️ Warning if <50 |
| 100-150  | 55          | 50      | ⚠️ Warning if <50 |
| 150-200  | 55          | 50      | ⚠️ Warning if <50 |
| 200+     | 55          | 50      | ⚠️ Warning if <50 |

---

### Метрика 2: Memory usage

**Как измерить:**

```javascript
// В консоли браузера:
console.memory.usedJSHeapSize / 1048576 // MB

// Или использовать DevTools Memory tab:
// 1. DevTools → Memory
// 2. Take heap snapshot
// 3. Смотреть total size
```

**Целевой показатель:**

| Маркеров | Целевое использование |
|----------|----------------------|
| 50       | <80 MB               |
| 100      | <120 MB              |
| 150      | <140 MB              |
| 200      | <160 MB              |
| 300      | <200 MB              |

---

### Метрика 3: DOM элементов

**Как измерить:**

```javascript
// В консоли браузера:
document.querySelectorAll('*').length  // Всего элементов

// Или через DevTools:
// DevTools → Elements → right-click on body → Inspect
// Считать элементы в дереве
```

**Целевой показатель:**

| Маркеров | Advanced (DOM) | Canvas | Целевой Canvas |
|----------|---|---|---|
| 50       | 50-70 | N/A | <100 |
| 100      | 80-100 | N/A | <100 |
| 150      | 100-150 | 1 | <100 |
| 200      | N/A | 1 | <100 |

---

### Метрика 4: Paint/Composite time

**Как измерить:**

```
DevTools → Performance таб → Hit record → Pan/Zoom → Stop

Смотреть:
- Paint (зеленый) - должно быть <5ms
- Composite (фиолетовый) - должно быть <1ms
- Rendering в Main thread - должно быть <3ms
```

**Целевой показатель:**

| Операция | Текущее | После оптимизации |
|----------|---------|-------------------|
| Paint time | 5-10ms | <2ms |
| Composite | 1-3ms | 1ms |
| Rendering | 3-5ms | <1ms |

---

## 🧪 СПОСОБ ТЕСТИРОВАНИЯ

### Тест 1: Генерирование тестовых данных

**Скрипт для создания 100+ маркеров:**

```typescript
// Выполнить в консоли браузера:

// Способ 1: Через API
async function generateTestLocations(count = 100) {
  for (let i = 0; i < count; i++) {
    const response = await fetch('/api/locations', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        name: `Test Location ${i}`,
        type: 'workstation',
        status: 'available',
        floor: '5',
        x: Math.random() * 100,
        y: Math.random() * 100,
        capacity: Math.floor(Math.random() * 4) + 1,
      })
    });
    console.log(`Created: ${i}/${count}`);
  }
  window.location.reload();
}

generateTestLocations(100);

// Способ 2: Если есть endpoint для генерирования
await fetch('/api/locations/generate-test', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ count: 150, floor: '5' })
});
```

---

### Тест 2: Базовый performance test

```typescript
// Выполнить в консоли браузера:

async function performanceTest() {
  console.log('=== Performance Test Started ===');
  
  // Screenshot 1: Baseline
  console.log('Baseline metrics (no interaction):');
  console.log(`DOM nodes: ${document.querySelectorAll('*').length}`);
  console.log(`Memory: ${(console.memory?.usedJSHeapSize / 1048576).toFixed(1)} MB`);
  
  // Test pan
  console.log('\n--- Testing PAN ---');
  const mapContainer = document.querySelector('[data-testid="office-map"]');
  
  let frameCount = 0;
  let minFps = 60;
  
  const countFrames = () => {
    frameCount++;
    requestAnimationFrame(countFrames);
  };
  
  let lastTime = performance.now();
  countFrames();
  
  // Simulate pan (drag)
  for (let i = 0; i < 100; i++) {
    mapContainer?.dispatchEvent(new MouseEvent('mousemove', {
      clientX: 100 + i * 5,
      clientY: 100,
      bubbles: true
    }));
    await new Promise(r => setTimeout(r, 16)); // 60ms
  }
  
  const elapsed = performance.now() - lastTime;
  const fps = Math.round((frameCount * 1000) / elapsed);
  
  console.log(`FPS during pan: ${fps}`);
  console.log(`Total frames: ${frameCount}`);
  
  // Test zoom
  console.log('\n--- Testing ZOOM ---');
  // ... similar logic
  
  console.log('\n=== Test Complete ===');
}

performanceTest();
```

---

### Тест 3: Stress test

```typescript
// Stress test с 200+ маркеров:

async function stressTest() {
  // Сгенерировать 250 маркеров
  await generateTestLocations(250);
  
  // Дождаться загрузки и рендеринга
  await new Promise(r => setTimeout(r, 3000));
  
  // Запустить performance monitor
  console.log('Stress test with 250 markers');
  
  // Pan
  console.time('Pan 250 markers');
  // ... pan logic ...
  console.timeEnd('Pan 250 markers');
  
  // Zoom
  console.time('Zoom 250 markers');
  // ... zoom logic ...
  console.timeEnd('Zoom 250 markers');
}
```

---

## 📋 ЧЕКЛИСТ ТЕСТИРОВАНИЯ

### До применения оптимизаций

- [ ] **100 маркеров**
  - [ ] FPS при пании: ______ (целевой: 50-60)
  - [ ] DOM nodes: ______ (целевой: <100)
  - [ ] Memory: ______ MB (целевой: <120)
  - [ ] Paint time: ______ms (целевой: <10)

- [ ] **150 маркеров**
  - [ ] FPS при пании: ______ (целевой: 50-60)
  - [ ] DOM nodes: ______ (целевой: <100)
  - [ ] Memory: ______ MB (целевой: <150)

- [ ] **200 маркеров**
  - [ ] FPS при пании: ______ (целевой: 55-60)
  - [ ] Memory: ______ MB (целевой: <160)

### После применения оптимизаций

- [ ] **100 маркеров**
  - [ ] FPS при пании: ______ (должно быть 55-60 ✅)
  - [ ] DOM nodes: ______ (должно быть <100 ✅)
  - [ ] Memory: ______ MB (должно быть <120 ✅)

- [ ] **150 маркеров**
  - [ ] FPS при пании: ______ (должно быть 55-60 ✅)
  - [ ] Memory: ______ MB (должно быть <150 ✅)

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Фаза 1: Понизить порог Canvas (5 минут)

**ДО:**
```
100 маркеров (Advanced):
  FPS: 28 ❌
  Memory: 145 MB
  DOM nodes: 95
```

**ПОСЛЕ:**
```
100 маркеров (Canvas):
  FPS: 58 ✅ (+107%)
  Memory: 115 MB (-20%)
  DOM nodes: 1 ✅ (-99%)
```

### Фаза 2: Hover detection оптимизация (20 минут)

**Улучшение:**
- Hover responsiveness: +15%
- FPS: +5%

### Фаза 3: useTransition (15 минут)

**Улучшение:**
- UI smoothness: +20%
- Better frame distribution

### Итоговый результат:

```
╔════════════════════════════════════════════════════════╗
║           BEFORE vs AFTER ОПТИМИЗАЦИЯ                   ║
╠════════┬──────────────┬──────────────┬─────────────────╣
║ Метрика│ ДО (Текущ.)  │ ПОСЛЕ        │ Улучшение       ║
╠════════╪══════════════╪══════════════╪═════════════════╣
║100 мар.║ 28 FPS       │ 58 FPS       │ +107% 🚀        ║
║150 мар.║ 22 FPS       │ 58 FPS       │ +164% 🚀🚀      ║
║200 мар.║ 18 FPS       │ 57 FPS       │ +217% 🚀🚀🚀    ║
║Memory  │ ~150 MB      │ ~120 MB      │ -20% ✅         ║
║DOM el. │ 90-150       │ 1            │ -99% ✅         ║
╚════════╧══════════════╧══════════════╧═════════════════╝
```

---

## 📊 ГРАФИК УЛУЧШЕНИЯ

```
FPS Timeline (при пании на 100 маркерах):

ДО ОПТИМИЗАЦИИ:
───────────────
60 |       
50 |   ╱─╲     ╱─╲     ╱─╲
40 |  ╱   ╲   ╱   ╲   ╱
30 |_╱─────╲_╱─────╲_╱─── (~28 FPS average, видны скачки)
   0    1    2    3    4   (Time, sec)

ПОСЛЕ ОПТИМИЗАЦИИ (Фаза 1):
──────────────────────────
60 |──────────────────────── (58 FPS stable) ✅
50 |
40 |
30 |

ПОСЛЕ ПОЛНОЙ ОПТИМИЗАЦИИ (Фазы 1-3):
────────────────────────────────────
60 |===================================== (60 FPS locked) ✅✅

Видимые улучшения:
  - Фразы убраны
  - FPS стабилен на 60 (или 58-59)
  - Никакихзаиканий
```

---

## 🔍 КАК ВЫЯВИТЬ УЗКИЕ МЕСТА

### Использовать Chrome DevTools на фазе тестирования:

```
1. Открыть DevTools (F12)
2. Go to Performance tab
3. Record (Ctrl+Shift+E)
4. Pan/zoom на 100+ маркерах (5-10 сек)
5. Stop recording

Анализировать:
├─ Main thread (если красная линия высокая - проблема в JS)
├─ Rendering
│  ├─ Layout (если высокая - проблема в DOM)
│  ├─ Paint (если высокая - проблема в рендеринге)
│  └─ Composite (если низкая - OK)
├─ FPS meter (должна быть зеленая и ровная)
└─ Long tasks (должны быть <50ms)
```

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ ВАЛИДАЦИИ

- [ ] Изменены пороги Canvas (150 → 90)
- [ ] Тестирование на 100 маркерах показывает ✅ 55-60 FPS
- [ ] Тестирование на 150 маркерах показывает ✅ 55-60 FPS
- [ ] Тестирование на 200 маркерах показывает ✅ 55-60 FPS
- [ ] Memory usage в норме (<160 MB for 200 markers)
- [ ] Hover detection реагирует мгновенно (<5ms)
- [ ] Нет видимых фризов при пании/зуме
- [ ] Нет предупреждений в console
- [ ] На мобильных устройствах: 45-50 FPS (acceptable)

---

## 🎉 РЕЗУЛЬТАТ

После применения Фазы 1 (5 минут работы) проблема должна быть **полностью решена** для 100+ маркеров.

Дополнительные фазы обеспечивают дополнительное улучшение и адаптивность системы.
