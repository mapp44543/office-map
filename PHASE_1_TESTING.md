# ✅ ФАЗА 1: ИНСТРУКЦИИ ПО ТЕСТИРОВАНИЮ

**Дата:** 2 марта 2026  
**Изменение:** Понизили пороги Canvas/Advanced (150→90, 80→50)  
**Ожидаемый результат:** +100-150% улучшение FPS при 100-150 маркерах

---

## 📋 ШАГИ ТЕСТИРОВАНИЯ

### Шаг 1: Подготовка окружения

1. ✅ Обновил пороги в `client/src/components/office-map.tsx`
2. Сохрани файл (Ctrl+S)
3. Перезагрузи страницу браузера (Ctrl+Shift+R - полная перезагрузка)

---

### Шаг 2: Генерирование тестовых данных (100 маркеров)

**Способ 1: Через API запрос** (если есть endpoint)

```bash
# В терминале:
curl -X POST http://localhost:5000/api/locations/generate-test \
  -H "Content-Type: application/json" \
  -d '{"count": 100, "floor": "5"}'

# Или вручную через браузер console - смотри ниже
```

**Способ 2: Через консоль браузера**

```javascript
// 1. Открой DevTools (F12)
// 2. Перейди в Console таб
// 3. Скопируй и выполни этот код:

async function createTestLocations(count = 100) {
  console.log(`Creating ${count} test locations...`);
  let created = 0;
  
  for (let i = 0; i < count; i++) {
    try {
      const response = await fetch('/api/locations', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          name: `Test Location ${i + 1}`,
          type: 'workstation',
          status: ['available', 'occupied', 'maintenance'][Math.floor(Math.random() * 3)],
          floor: '5',
          x: Math.random() * 100,
          y: Math.random() * 100,
          capacity: Math.floor(Math.random() * 4) + 1,
          equipment: `PC-${Math.floor(Math.random() * 100)}`,
        })
      });
      
      if (response.ok) {
        created++;
        if (created % 10 === 0) console.log(`✓ Created ${created}/${count}`);
      }
    } catch (e) {
      console.error(`Error creating location ${i}:`, e);
    }
  }
  
  console.log(`✅ Created ${created}/${count} locations`);
  console.log('Reloading page...');
  setTimeout(() => location.reload(), 1000);
}

createTestLocations(100);
```

**Ожидаемый результат:**
```
✓ Created 10/100
✓ Created 20/100
...
✓ Created 100/100
✅ Created 100/100 locations
Reloading page...
```

---

### Шаг 3: Измерение производительности ПЕРЕД исправлением (контроль)

> **Примечание:** Если ты уже перезагрузил с новым кодом - этот шаг можно пропустить,
> но желательно сравнить с бэкапом старого кода.

---

### Шаг 4: Проверка текущего FPS (ПОСЛЕ изменения)

**Способ 1: Chrome DevTools Performance Monitor**

```
1. Открой DevTools (F12)
2. Нажми ⋮ (три точки) → More tools → Performance Monitor
3. Смотри в real-time:
   - CPU usage
   - Memory usage
   - FPS (должно быть 55-60)
4. Выполни действия:
   - Pan (перемещение) - держи и тащи карту
   - Zoom (зум) - крути колесико мыши
5. Фризы НЕ должны быть видимы
```

**Ожидаемый результат:**
```
Memory: 110-130 MB (было: 140-160 MB)
FPS: 55-60 ✅ (было: 25-40)
DOM Nodes: 1-5 (было: 80-150)
```

**Способ 2: Вручную через консоль**

```javascript
// Скопируй в консоль:

let frameCount = 0;
let lastTime = performance.now();
let frameLog = [];

function measureFPS() {
  frameCount++;
  requestAnimationFrame(measureFPS);
}

measureFPS();

// Подожди 10 секунд и выполни:
setTimeout(() => {
  const elapsed = performance.now() - lastTime;
  const fps = Math.round((frameCount * 1000) / elapsed);
  console.log(`📊 FPS Average: ${fps}`);
  console.log(`📊 Total frames in ${Math.round(elapsed)}ms: ${frameCount}`);
  console.log(`📊 Memory: ${(performance.memory?.usedJSHeapSize / 1048576).toFixed(1)} MB`);
  console.log(`📊 DOM nodes: ${document.querySelectorAll('*').length}`);
  
  if (fps >= 55) {
    console.log('✅ РЕЗУЛЬТАТ: Отлично! FPS в норме');
  } else if (fps >= 50) {
    console.log('⚠️ РЕЗУЛЬТАТ: Приемлемо, но можно еще улучшить');
  } else {
    console.log('❌ РЕЗУЛЬТАТ: Проблема остается, нужна доп. оптимизация');
  }
}, 10000);

console.log('Measuring FPS... Perform pan/zoom actions now. Results in 10 sec.');
```

**При пании на 100 маркерах - ожидаемый результат:**
```
📊 FPS Average: 58
📊 Total frames in ~10000ms: 580
📊 Memory: 115.3 MB
📊 DOM nodes: 4
✅ РЕЗУЛЬТАТ: Отлично! FPS в норме
```

---

### Шаг 5: Тестирование разных количеств маркеров

| Маркеров | Ожидаемый режим | Ожидаемый FPS | Статус |
|----------|---|---|---|
| 50 | Basic | 60 | ✅ |
| 80 | Advanced | 55-60 | ✅ |
| 100 | **Canvas** | **55-60** | ✅ NEW! |
| 150 | Canvas | 55-60 | ✅ |
| 200 | Canvas | 55-60 | ✅ |

**Тестирование:**

```javascript
// Проверь режим на этапе выбора
// В console выполни:

// Получи текущее количество маркеров
const markerCount = document.querySelectorAll('[data-testid*="location-marker"]').length 
                 || document.querySelectorAll('circle').length 
                 || 'Unknown';

console.log(`Current markers on page: ${markerCount}`);

// Проверь какой элемент используется для рендеринга:
const hasDOMMarkers = document.querySelectorAll('[data-testid*="location-marker"]').length > 0;
const hasCanvas = document.querySelector('canvas[data-testid*="canvas"]');

if (hasCanvas) {
  console.log('✅ Using Canvas mode (expected for 90+ markers)');
} else if (hasDOMMarkers) {
  console.log('⚠️ Using DOM mode (ok for <90 markers)');
} else {
  console.log('❓ Unknown rendering mode');
}
```

---

### Шаг 6: Профилирование через Chrome DevTools

**Для детального анализа:**

```
1. F12 → Performance таб
2. Нажми record (красный circle)
3. Выполни: Pan на карте (~5 сек)
4. Stop recording
5. Анализируй результаты:
   - Main thread (должна быть низко)
   - Rendering (должна быть низко)
   - FPS meter (должна быть ровная зеленая линия)
```

**Ожидаемая диаграмма:**

```
ПОСЛЕ исправления (100 маркеров, Canvas режим):

60 |━━━━━━━━━━━━━━━━━━━━━━━ (ровная линия = 58-60 FPS)
50 |
40 |
30 |
20 |
    0s    1s    2s    3s    4s    5s    (Time during pan)

Main thread: ▁▁▂▁▁▂▁▁▂▁▁▂ (низкая активность)
Rendering:   ▁▂▁▂▁▂▁▂▁▂▁▂ (равномерная)
```

---

## ✅ ЧЕКЛИСТ УСПЕХА

После Фазы 1 должны выполниться:

- [ ] Файл `office-map.tsx` обновлен (пороги 150→90, 80→50)
- [ ] Браузер перезагружен (Ctrl+Shift+R)
- [ ] Созданы 100 тестовых маркеров
- [ ] Нет видимых фризов при пании/зуме
- [ ] FPS стабилен на 55-60 (проверяется через Performance Monitor)
- [ ] DOM nodes < 10 (проверяется в DevTools)
- [ ] Memory < 130 MB
- [ ] Canvas режим активен для 100 маркеров

---

## 🔍 ОТЛАДКА (если что-то не работает)

### Проблема: Фризы все еще видны

**Решение:**
```javascript
// Проверь, какой режим используется
const renderModeIndicator = document.querySelector('[data-testid="render-mode"]');
console.log('Current render mode:', renderModeIndicator?.textContent || 'indicator not found');

// Проверь количество DOM элементов
console.log('DOM markers:', document.querySelectorAll('[data-testid*="marker"]').length);
console.log('Canvas elements:', document.querySelectorAll('canvas').length);
```

**Если Canvas не используется при 100 маркерах:**
1. Проверь консоль на ошибки (F12 → Console)
2. Убедись что все файлы закэшированы (Ctrl+Shift+R)
3. Проверь что обновление кода сохранилось в office-map.tsx

### Проблема: FPS ниже 50

**Решение:**
1. Перезагрузи браузер (Ctrl+Shift+R)
2. Закрой другие вкладки
3. Отключи DevTools (F12)
4. Повтори тестирование

### Проблема: Memory растет

**Решение:**
```javascript
// Принудительно запусти garbage collection
// (только если DevTools открит в режиме инструментов памяти)
// В Memory таб нажми garbage bin icon
```

---

## 📊 ДОКУМЕНТИРОВАНИЕ РЕЗУЛЬТАТОВ

После тестирования скопируй результаты сюда (для отчета):

```
=== ТЕСТИРОВАНИЕ ФАЗЫ 1 ===
Дата: ____________
Браузер: ____________
Устройство: ____________

МЕТРИКИ (100 маркеров):
- FPS при пании: ______ (ожидаемый: 55-60)
- FPS при зуме: ______ (ожидаемый: 55-60)
- Memory: ______ MB (ожидаемый: <130)
- DOM nodes: ______ (ожидаемый: <10)
- Render mode: ______ (ожидаемый: Canvas)

СУБЪЕКТИВНОЕ ВПЕЧАТЛЕНИЕ:
- Заикания: ДА/НЕТ
- Лаги при hover: ДА/НЕТ
- Плавность pan/zoom: Плохая/Средняя/Хорошая/Отличная

СТАТУС: ✅ OK / ⚠️ Needs improvement / ❌ Failed
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После успешного прохождения Фазы 1:

1. **Документировать результаты**
2. **Если результат положительный:** Переходить на Фазу 2 (hover detection)
3. **Если результат не соответствует ожиданиям:** Отладить и повторить

---

## 📞 ПОМОЩЬ

Если что-то не работает как ожидается:
1. Проверь что обновлены пороги в коде
2. Убедись что файл сохранен
3. Полная перезагрузка браузера (Ctrl+Shift+R)
4. Проверь консоль на ошибки
5. Смотри раздел ОТЛАДКА выше

**Успех! 🚀**
